# AI 动态角色状态

安装并启用后，NativeTavern 会在每次角色回复完成后使用当前模型分析最近剧情，并维护该角色在当前对话中的状态。

- 属性由 AI 根据角色和剧情自由创建、修改或移除。
- 每个对话、每个角色各自保存一份状态；群聊角色互不混淆。
- 点击角色头像下方的“状态”按钮即可查看。
- 状态生成失败不会影响正常聊天，并会继续显示上一次成功保存的状态。

该插件使用 NativeTavern 的受控 `character-status-v1` 能力，不包含或执行第三方代码。
