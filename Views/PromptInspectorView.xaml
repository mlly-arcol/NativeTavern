<UserControl x:Class="NativeTavern.Views.PromptInspectorView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
 <Grid Margin="28"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
  <DockPanel Margin="0,0,0,18"><Button DockPanel.Dock="Right" Content="Refresh" Command="{Binding RefreshCommand}" Margin="0"/><StackPanel><TextBlock Text="Prompt Inspector" FontSize="28" FontWeight="SemiBold"/><TextBlock Text="Inspect the exact text context before sending it to your Provider." Foreground="{StaticResource MutedTextBrush}" Margin="0,5,0,0"/></StackPanel></DockPanel>
  <WrapPanel Grid.Row="1" Margin="0,0,0,12"><Border Background="{StaticResource CardBrush}" CornerRadius="6" Padding="10" Margin="0,0,10,0"><StackPanel Orientation="Horizontal"><TextBlock Text="Estimated tokens: "/><TextBlock Text="{Binding EstimatedTokens}" FontWeight="SemiBold"/></StackPanel></Border><Border Background="{StaticResource CardBrush}" CornerRadius="6" Padding="10"><StackPanel Orientation="Horizontal"><TextBlock Text="Lore: "/><TextBlock Text="{Binding ActivatedLore}" FontWeight="SemiBold"/></StackPanel></Border></WrapPanel>
  <TextBox Grid.Row="2" Text="{Binding PromptText}" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="13"/>
  <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Foreground="{StaticResource MutedTextBrush}" Margin="0,12,0,0"/>
 </Grid>
</UserControl>
