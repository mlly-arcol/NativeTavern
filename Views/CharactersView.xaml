<UserControl x:Class="NativeTavern.Views.CharactersView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
 <UserControl.Resources>
  <Style x:Key="CharacterListItemStyle" TargetType="ListBoxItem">
   <Setter Property="HorizontalContentAlignment" Value="Stretch"/><Setter Property="Padding" Value="0"/>
   <Setter Property="Margin" Value="0,0,0,8"/><Setter Property="Background" Value="Transparent"/>
   <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
   <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ListBoxItem">
    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}" SnapsToDevicePixels="True"/>
   </ControlTemplate></Setter.Value></Setter>
  </Style>
  <DataTemplate x:Key="CharacterCardTemplate">
   <Border x:Name="CharacterCard" Background="Transparent" BorderBrush="Transparent" BorderThickness="1" CornerRadius="11" Padding="10">
    <Grid>
     <Grid.ColumnDefinitions><ColumnDefinition Width="50"/><ColumnDefinition Width="*"/><ColumnDefinition Width="34"/></Grid.ColumnDefinitions>
     <Grid Width="44" Height="44" VerticalAlignment="Center">
      <Ellipse Fill="{StaticResource CardBrush}"/>
      <Ellipse><Ellipse.Fill><ImageBrush ImageSource="{Binding AvatarPath}" Stretch="UniformToFill"/></Ellipse.Fill></Ellipse>
      <TextBlock Text="{Binding Name, Converter={StaticResource InitialsConverter}}" FontWeight="SemiBold" FontSize="14"
                 Foreground="{StaticResource MutedTextBrush}" HorizontalAlignment="Center" VerticalAlignment="Center">
       <TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed"/>
        <Style.Triggers><DataTrigger Binding="{Binding AvatarPath}" Value=""><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers>
       </Style></TextBlock.Style>
      </TextBlock>
     </Grid>
     <StackPanel Grid.Column="1" Margin="9,2,8,1" VerticalAlignment="Center">
      <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14" TextTrimming="CharacterEllipsis"/>
      <TextBlock Text="{Binding Tags}" Foreground="{StaticResource MutedTextBrush}" FontSize="11" TextTrimming="CharacterEllipsis" Margin="0,5,0,0"/>
     </StackPanel>
     <Button Grid.Column="2" Content="★" Width="30" Height="30" Padding="0" Margin="0" Background="Transparent" BorderThickness="0"
             Command="{Binding DataContext.ToggleFavoriteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
      <Button.Style><Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
       <Setter Property="Opacity" Value="0.28"/><Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True"><Setter Property="Opacity" Value="0.7"/></Trigger>
        <DataTrigger Binding="{Binding IsFavorite}" Value="True"><Setter Property="Opacity" Value="1"/><Setter Property="Foreground" Value="#D99A00"/></DataTrigger>
       </Style.Triggers>
      </Style></Button.Style>
     </Button>
    </Grid>
   </Border>
   <DataTemplate.Triggers><DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
    <Setter TargetName="CharacterCard" Property="Background" Value="#F3F6FA"/><Setter TargetName="CharacterCard" Property="BorderBrush" Value="#70A4D6"/>
   </DataTrigger></DataTemplate.Triggers>
  </DataTemplate>
 </UserControl.Resources>
 <Grid Margin="28">
  <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
  <DockPanel Margin="0,0,0,18">
   <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
    <Button Content="Import Card" Click="Import_OnClick"/>
    <Button Content="+ New Character" Command="{Binding NewCharacterCommand}" Style="{StaticResource PrimaryButtonStyle}"/>
   </StackPanel>
   <StackPanel><TextBlock Text="Characters" FontSize="28" FontWeight="SemiBold"/><TextBlock Text="Create, import and manage character cards" Foreground="{StaticResource MutedTextBrush}" Margin="0,5,0,0"/></StackPanel>
  </DockPanel>
  <Grid Grid.Row="1">
   <Grid.ColumnDefinitions><ColumnDefinition Width="360"/><ColumnDefinition Width="10"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
   <Border Grid.Column="0" Style="{StaticResource SurfaceBorderStyle}" Padding="14">
    <Grid>
     <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
     <Grid>
      <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" ToolTip="Search name, tags or description"/>
      <TextBlock Text="Search characters" Foreground="{StaticResource SubtleTextBrush}" Margin="12,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False">
       <TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed"/>
        <Style.Triggers><DataTrigger Binding="{Binding SearchText}" Value=""><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers>
       </Style></TextBlock.Style>
      </TextBlock>
     </Grid>
     <DockPanel Grid.Row="1" Margin="3,12,0,11">
      <Button DockPanel.Dock="Right" Content="+ 创建分组" Click="CreateGroup_OnClick" Padding="11,6" Margin="8,0,0,0"/>
      <CheckBox Content="Favorites only" IsChecked="{Binding FavoritesOnly}" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center"/>
     </DockPanel>
     <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
      <StackPanel>
       <ItemsControl ItemsSource="{Binding CharacterGroups}">
        <ItemsControl.ItemTemplate><DataTemplate>
         <StackPanel Margin="0,0,0,8">
          <Border Background="{StaticResource CardBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="11" Padding="5">
           <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="48"/><ColumnDefinition Width="34"/><ColumnDefinition Width="34"/></Grid.ColumnDefinitions>
            <Button Grid.Column="0" Command="{Binding DataContext.ToggleGroupCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    CommandParameter="{Binding}" Background="Transparent" BorderThickness="0" Padding="5,6" Margin="0" HorizontalContentAlignment="Stretch">
             <Grid>
              <Grid.ColumnDefinitions><ColumnDefinition Width="22"/><ColumnDefinition Width="29"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
              <TextBlock VerticalAlignment="Center" Foreground="{StaticResource MutedTextBrush}">
               <TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Text" Value="▶"/>
                <Style.Triggers><DataTrigger Binding="{Binding IsExpanded}" Value="True"><Setter Property="Text" Value="▼"/></DataTrigger></Style.Triggers>
               </Style></TextBlock.Style>
              </TextBlock>
              <TextBlock Grid.Column="1" Text="📁" FontSize="18" VerticalAlignment="Center"/>
              <TextBlock Grid.Column="2" Text="{Binding Name}" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
              <Border Grid.Column="3" Background="White" CornerRadius="9" Padding="7,2" Margin="8,0,2,0">
               <TextBlock Text="{Binding CharacterCount}" FontSize="11" Foreground="{StaticResource MutedTextBrush}"/>
              </Border>
             </Grid>
            </Button>
            <Button Grid.Column="1" Content="群聊" ToolTip="使用该分组创建群聊" Click="StartGroupChat_OnClick" Padding="3" Margin="1"/>
            <Button Grid.Column="2" Content="✎" ToolTip="编辑分组" Click="EditGroup_OnClick" Padding="0" Margin="1"/>
            <Button Grid.Column="3" Content="×" ToolTip="删除分组" Click="DeleteGroup_OnClick" Padding="0" Margin="1"
                    Foreground="#B42318"/>
           </Grid>
          </Border>
          <ListBox ItemsSource="{Binding Characters}"
                   SelectedItem="{Binding DataContext.SelectedCharacter, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                   ItemTemplate="{StaticResource CharacterCardTemplate}" ItemContainerStyle="{StaticResource CharacterListItemStyle}"
                   Background="Transparent" BorderThickness="0" Margin="18,8,0,0"
                   ScrollViewer.VerticalScrollBarVisibility="Disabled" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
           <ListBox.Style><Style TargetType="ListBox"><Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers><DataTrigger Binding="{Binding IsExpanded}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers>
           </Style></ListBox.Style>
          </ListBox>
         </StackPanel>
        </DataTemplate></ItemsControl.ItemTemplate>
       </ItemsControl>
       <ListBox ItemsSource="{Binding UngroupedCharacters}" SelectedItem="{Binding SelectedCharacter, Mode=TwoWay}"
                ItemTemplate="{StaticResource CharacterCardTemplate}" ItemContainerStyle="{StaticResource CharacterListItemStyle}"
                Background="Transparent" BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Disabled"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled"/>
      </StackPanel>
     </ScrollViewer>
    </Grid>
   </Border>
   <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Center" Background="{StaticResource BorderBrush}"/>
   <Border Grid.Column="2" Style="{StaticResource SurfaceBorderStyle}" Padding="0">
    <Grid>
     <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
     <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="22,18">
      <Grid>
       <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
       <Grid Width="88" Height="88">
        <Ellipse Fill="{StaticResource CardBrush}"/>
        <Ellipse>
         <Ellipse.Fill><ImageBrush ImageSource="{Binding AvatarPath}" Stretch="UniformToFill"/></Ellipse.Fill>
        </Ellipse>
       </Grid>
       <StackPanel Grid.Column="1" Margin="18,8,18,0">
        <TextBlock Text="{Binding Name}" FontSize="20" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
        <TextBlock Text="{Binding Creator}" Foreground="{StaticResource MutedTextBrush}" FontSize="12" Margin="0,7,0,0" TextTrimming="CharacterEllipsis"/>
       </StackPanel>
       <StackPanel Grid.Column="2" VerticalAlignment="Center">
        <Button Content="Choose Avatar" Click="ChooseAvatar_OnClick" Margin="0,0,0,11"/>
        <CheckBox Content="Favorite" IsChecked="{Binding IsFavorite}" Foreground="{StaticResource TextBrush}" HorizontalAlignment="Center"/>
       </StackPanel>
      </Grid>
     </Border>
     <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="22,18,18,14">
      <Grid>
       <Grid.ColumnDefinitions><ColumnDefinition Width="110"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
       <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
       <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>
       <TextBlock Grid.Row="0" Text="Name *" VerticalAlignment="Center"/><TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>
       <TextBlock Grid.Row="1" Text="Creator" VerticalAlignment="Center"/><TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Creator, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>
       <TextBlock Grid.Row="2" Text="Tags" VerticalAlignment="Center"/><TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Tags, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>
       <TextBlock Grid.Row="3" Text="Group" VerticalAlignment="Center"/>
       <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding GroupName}" IsReadOnly="True"
                ToolTip="Use the group editor to change membership" Margin="0,0,0,12"/>
       <TextBlock Grid.Row="4" Text="Description" VerticalAlignment="Top" Margin="0,10,0,0"/><TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="84" Margin="0,0,0,12"/>
       <TextBlock Grid.Row="5" Text="Personality" VerticalAlignment="Top" Margin="0,10,0,0"/><TextBox Grid.Row="5" Grid.Column="1" Text="{Binding Personality, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="84" Margin="0,0,0,12"/>
       <TextBlock Grid.Row="6" Text="Scenario" VerticalAlignment="Top" Margin="0,10,0,0"/><TextBox Grid.Row="6" Grid.Column="1" Text="{Binding Scenario, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="84" Margin="0,0,0,12"/>
       <TextBlock Grid.Row="7" Text="First Message" VerticalAlignment="Top" Margin="0,10,0,0"/><TextBox Grid.Row="7" Grid.Column="1" Text="{Binding FirstMessage, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="118" Margin="0,0,0,12"/>
       <TextBlock Grid.Row="8" Text="Example Messages" VerticalAlignment="Top" Margin="0,10,0,0"/><TextBox Grid.Row="8" Grid.Column="1" Text="{Binding ExampleMessages, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="118"/>
      </Grid>
     </ScrollViewer>
     <Border Grid.Row="2" Background="#FAFAFA" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="22,14">
      <Grid>
       <StackPanel Orientation="Horizontal">
       <Button Content="Save" Command="{Binding SaveCommand}" Style="{StaticResource PrimaryButtonStyle}"/>
       <Button Content="Start Chat" Command="{Binding StartChatCommand}"/>
       <Button Content="Delete" Click="Delete_OnClick" Background="#FFF3F2" Foreground="#B42318" BorderBrush="#F1C6C2"/>
       </StackPanel>
       <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="16,0,0,0"/>
      </Grid>
     </Border>
    </Grid>
   </Border>
  </Grid>
 </Grid>
</UserControl>
