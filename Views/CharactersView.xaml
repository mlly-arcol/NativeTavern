<UserControl x:Class="NativeTavern.Views.CharactersView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
 <Grid Margin="28">
  <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
  <DockPanel Margin="0,0,0,18">
   <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
    <Button Content="Import Card" Click="Import_OnClick"/>
    <Button Content="+ New Character" Command="{Binding NewCharacterCommand}" Style="{StaticResource PrimaryButtonStyle}"/>
   </StackPanel>
   <StackPanel><TextBlock Text="Characters" FontSize="28" FontWeight="SemiBold"/><TextBlock Text="Create, import and manage character cards" Foreground="{StaticResource MutedTextBrush}" Margin="0,5,0,0"/></StackPanel>
  </DockPanel>
  <Grid Grid.Row="1">
   <Grid.ColumnDefinitions><ColumnDefinition Width="330"/><ColumnDefinition Width="8"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
   <Border Grid.Column="0" Style="{StaticResource SurfaceBorderStyle}" Padding="14">
    <Grid>
     <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
     <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" ToolTip="Search name, tags or description"/>
     <CheckBox Grid.Row="1" Content="Favorites only" IsChecked="{Binding FavoritesOnly}" Foreground="{StaticResource TextBrush}" Margin="2,12"/>
     <ListBox Grid.Row="2" ItemsSource="{Binding Characters}" SelectedItem="{Binding SelectedCharacter}" Background="Transparent" BorderThickness="0">
      <ListBox.ItemContainerStyle><Style TargetType="ListBoxItem"><Setter Property="HorizontalContentAlignment" Value="Stretch"/><Setter Property="Padding" Value="4"/><Setter Property="Margin" Value="0,0,0,7"/></Style></ListBox.ItemContainerStyle>
      <ListBox.ItemTemplate><DataTemplate>
       <Grid Margin="4"><Grid.ColumnDefinitions><ColumnDefinition Width="58"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
        <Border Width="50" Height="50" CornerRadius="7" Background="{StaticResource CardBrush}" ClipToBounds="True"><Image Source="{Binding AvatarPath}" Stretch="UniformToFill"/></Border>
        <StackPanel Grid.Column="1" Margin="10,2"><TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/><TextBlock Text="{Binding Tags}" Foreground="{StaticResource MutedTextBrush}" TextTrimming="CharacterEllipsis" Margin="0,5,0,0"/></StackPanel>
        <Button Grid.Column="2" Content="★" Padding="8,4" Command="{Binding DataContext.ToggleFavoriteCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}">
         <Button.Style><Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}"><Setter Property="Opacity" Value="0.35"/><Style.Triggers><DataTrigger Binding="{Binding IsFavorite}" Value="True"><Setter Property="Opacity" Value="1"/><Setter Property="Foreground" Value="#FFD166"/></DataTrigger></Style.Triggers></Style></Button.Style>
        </Button>
       </Grid>
      </DataTemplate></ListBox.ItemTemplate>
     </ListBox>
    </Grid>
   </Border>
   <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Center" Background="{StaticResource BorderBrush}"/>
   <Border Grid.Column="2" Style="{StaticResource SurfaceBorderStyle}" Padding="22">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
     <Grid>
      <Grid.ColumnDefinitions><ColumnDefinition Width="130"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
       <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
       <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
       <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>
      <StackPanel Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,0,0,18">
       <Border Width="92" Height="92" CornerRadius="10" Background="{StaticResource CardBrush}" ClipToBounds="True"><Image Source="{Binding AvatarPath}" Stretch="UniformToFill"/></Border>
       <StackPanel Margin="16,0"><Button Content="Choose Avatar" Click="ChooseAvatar_OnClick"/><CheckBox Content="Favorite" IsChecked="{Binding IsFavorite}" Foreground="{StaticResource TextBrush}" Margin="3,12"/></StackPanel>
      </StackPanel>
      <TextBlock Grid.Row="1" Text="Name *" VerticalAlignment="Center"/><TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>
      <TextBlock Grid.Row="2" Text="Creator" VerticalAlignment="Center"/><TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Creator, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>
      <TextBlock Grid.Row="3" Text="Tags" VerticalAlignment="Center"/><TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Tags, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>
      <TextBlock Grid.Row="4" Text="Description" VerticalAlignment="Top" Margin="0,10,0,0"/><TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="72" Margin="0,0,0,12"/>
      <TextBlock Grid.Row="5" Text="Personality" VerticalAlignment="Top" Margin="0,10,0,0"/><TextBox Grid.Row="5" Grid.Column="1" Text="{Binding Personality, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="72" Margin="0,0,0,12"/>
      <TextBlock Grid.Row="6" Text="Scenario" VerticalAlignment="Top" Margin="0,10,0,0"/><TextBox Grid.Row="6" Grid.Column="1" Text="{Binding Scenario, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="72" Margin="0,0,0,12"/>
      <TextBlock Grid.Row="7" Text="First Message" VerticalAlignment="Top" Margin="0,10,0,0"/><TextBox Grid.Row="7" Grid.Column="1" Text="{Binding FirstMessage, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="92" Margin="0,0,0,12"/>
      <TextBlock Grid.Row="8" Text="Example Messages" VerticalAlignment="Top" Margin="0,10,0,0"/><TextBox Grid.Row="8" Grid.Column="1" Text="{Binding ExampleMessages, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="92" Margin="0,0,0,16"/>
      <StackPanel Grid.Row="9" Grid.Column="1" Orientation="Horizontal">
       <Button Content="Save" Command="{Binding SaveCommand}" Style="{StaticResource PrimaryButtonStyle}"/>
       <Button Content="Start Chat" Command="{Binding StartChatCommand}"/>
       <Button Content="Delete" Click="Delete_OnClick" Background="#FFF3F2" Foreground="#B42318" BorderBrush="#F1C6C2"/>
      </StackPanel>
      <TextBlock Grid.Row="10" Grid.Column="1" Text="{Binding StatusMessage}" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" Margin="0,14,0,4"/>
     </Grid>
    </ScrollViewer>
   </Border>
  </Grid>
 </Grid>
</UserControl>
