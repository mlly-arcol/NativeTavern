<UserControl x:Class="NativeTavern.Views.ChatView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:models="clr-namespace:NativeTavern.Models">
 <Grid Background="{StaticResource WindowBrush}">
  <Grid.RowDefinitions><RowDefinition Height="64"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>

  <!-- Compact conversation header -->
  <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="22,0">
   <Grid>
    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
    <StackPanel VerticalAlignment="Center">
     <TextBlock Text="{Binding SessionTitle}" FontSize="16" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
     <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
      <Ellipse Width="6" Height="6" Fill="{StaticResource AccentBrush}" VerticalAlignment="Center" Margin="0,0,6,0"/>
      <TextBlock Text="Local workspace" Foreground="{StaticResource MutedTextBrush}" FontSize="11"/>
      <TextBlock Text="  ·  ~" Foreground="{StaticResource SubtleTextBrush}" FontSize="11"/>
      <TextBlock Text="{Binding EstimatedPromptTokens}" Foreground="{StaticResource SubtleTextBrush}" FontSize="11"/>
      <TextBlock Text=" tokens" Foreground="{StaticResource SubtleTextBrush}" FontSize="11"/>
     </StackPanel>
    </StackPanel>
    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
     <ComboBox Width="230" Height="38" ItemsSource="{Binding Sessions}" SelectedItem="{Binding SelectedSession}" DisplayMemberPath="Title" Margin="0,0,8,0" ToolTip="Switch conversation"/>
     <Button Content="⌕" Style="{StaticResource RoundIconButtonStyle}" Command="{Binding OpenPromptInspectorCommand}" Margin="0,0,8,0" ToolTip="Inspect prompt"/>
     <Button Content="＋" Style="{StaticResource RoundIconButtonStyle}" Command="{Binding NewChatCommand}" Margin="0,0,8,0" ToolTip="New chat"/>
     <Button Content="⋯" Style="{StaticResource RoundIconButtonStyle}" Click="DeleteChat_OnClick" ToolTip="Delete conversation"/>
    </StackPanel>
   </Grid>
  </Border>

  <!-- Optional prompt context controls -->
  <Border Grid.Row="1" Background="#FAFAFA" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="20,5">
   <Grid MaxWidth="980" HorizontalAlignment="Stretch">
    <Grid.ColumnDefinitions><ColumnDefinition Width="135"/><ColumnDefinition Width="135"/><ColumnDefinition Width="155"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
    <ComboBox Height="32" MinHeight="0" Padding="9,5" ItemsSource="{Binding Personas}" SelectedItem="{Binding SelectedPersona}" DisplayMemberPath="Name" Tag="Persona" ToolTip="Persona" Margin="0,0,7,0"/>
    <ComboBox Grid.Column="1" Height="32" MinHeight="0" Padding="9,5" ItemsSource="{Binding Lorebooks}" SelectedItem="{Binding SelectedLorebook}" DisplayMemberPath="Name" Tag="Lorebook" ToolTip="Lorebook" Margin="0,0,7,0"/>
    <ComboBox Grid.Column="2" Height="32" MinHeight="0" Padding="9,5" ItemsSource="{Binding Presets}" SelectedItem="{Binding SelectedPreset}" DisplayMemberPath="Name" Tag="Prompt preset" ToolTip="Prompt preset" Margin="0,0,7,0"/>
    <Grid Grid.Column="3" Margin="0,0,7,0">
     <TextBox x:Name="AuthorNoteInput" Height="32" Padding="10,5" Text="{Binding AuthorNote,UpdateSourceTrigger=PropertyChanged}" ToolTip="Author note"/>
     <TextBlock Text="Author note" Foreground="{StaticResource MutedTextBrush}" Margin="10,0" VerticalAlignment="Center" IsHitTestVisible="False">
      <TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding AuthorNote}" Value=""><Setter Property="Visibility" Value="Visible"/></DataTrigger><DataTrigger Binding="{Binding AuthorNote}" Value="{x:Null}"><Setter Property="Visibility" Value="Visible"/></DataTrigger><DataTrigger Binding="{Binding IsKeyboardFocused, ElementName=AuthorNoteInput}" Value="True"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style></TextBlock.Style>
     </TextBlock>
    </Grid>
    <Button Grid.Column="4" Height="32" Content="Apply" Command="{Binding ApplyPromptContextCommand}" Padding="12,5"/>
    <Button Grid.Column="5" Height="32" Content="Clear" Command="{Binding ClearPromptContextCommand}" Style="{StaticResource GhostButtonStyle}" Padding="10,5" Margin="0"/>
   </Grid>
  </Border>

  <!-- Conversation stream -->
  <Grid Grid.Row="2">
   <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20,0,20,70" IsHitTestVisible="False">
    <Grid.Style>
     <Style TargetType="Grid"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding Messages.Count}" Value="0"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style>
    </Grid.Style>
    <StackPanel HorizontalAlignment="Center">
     <Border Width="48" Height="48" CornerRadius="16" Background="{StaticResource AccentBrush}" HorizontalAlignment="Center" Margin="0,0,0,18">
      <TextBlock Text="N" Foreground="White" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
     </Border>
     <TextBlock Text="今天想聊些什么？" FontSize="25" FontWeight="SemiBold" HorizontalAlignment="Center"/>
     <TextBlock Text="选择角色或直接输入消息，开始一段新的对话" FontSize="13" Foreground="{StaticResource MutedTextBrush}" HorizontalAlignment="Center" Margin="0,8,0,0"/>
    </StackPanel>
   </Grid>
   <Border Panel.ZIndex="5" Background="#FFF7D6" BorderBrush="#F0D98A" BorderThickness="1" CornerRadius="10" Padding="14" Margin="24,16" VerticalAlignment="Top" MaxWidth="900"
           Visibility="{Binding HasProviderConfiguration, Converter={StaticResource InverseBoolToVisibility}}">
    <DockPanel><Button DockPanel.Dock="Right" Content="Open settings" Command="{Binding OpenSettingsCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="12,0,0,0"/>
     <StackPanel><TextBlock Text="Connect a model to start chatting" FontWeight="SemiBold"/><TextBlock Text="Configure a local or cloud provider in Settings." Foreground="#765F18" FontSize="12" Margin="0,3,0,0"/></StackPanel></DockPanel>
   </Border>
        <ListBox x:Name="MessageList" ItemsSource="{Binding Messages}" Background="Transparent" BorderThickness="0"
                 Padding="0,20,0,12"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 ScrollViewer.CanContentScroll="True"
                 ScrollViewer.IsDeferredScrollingEnabled="True"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling"
                 VirtualizingPanel.ScrollUnit="Pixel"
                 VirtualizingPanel.CacheLength="1,2"
                 VirtualizingPanel.CacheLengthUnit="Page">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
    <ListBox.ItemContainerStyle>
     <Style TargetType="ListBoxItem">
      <Setter Property="HorizontalContentAlignment" Value="Stretch"/><Setter Property="Padding" Value="0"/>
      <Setter Property="Margin" Value="20,0,20,22"/><Setter Property="Background" Value="Transparent"/>
      <Setter Property="Focusable" Value="False"/>
      <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ListBoxItem"><ContentPresenter/></ControlTemplate></Setter.Value></Setter>
     </Style>
    </ListBox.ItemContainerStyle>
    <ListBox.ItemTemplate>
     <DataTemplate>
      <Border x:Name="MessageBubble" Padding="16,13" CornerRadius="18" MaxWidth="820">
       <Grid>
        <Grid.ColumnDefinitions><ColumnDefinition Width="32"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
        <Border x:Name="Avatar" Width="28" Height="28" CornerRadius="9" Background="{StaticResource AccentBrush}" VerticalAlignment="Top">
         <TextBlock x:Name="AvatarLetter" Text="N" Foreground="White" FontWeight="Bold" FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
        <StackPanel Grid.Column="1" Margin="10,0,0,0">
         <TextBlock Text="{Binding RoleLabel}" FontWeight="SemiBold" FontSize="13" Margin="0,3,0,7"/>
         <StackPanel Visibility="{Binding IsEditing, Converter={StaticResource InverseBoolToVisibility}}">
          <TextBlock Text="{Binding Content}" TextWrapping="Wrap" FontSize="15" LineHeight="23"/>
          <ItemsControl ItemsSource="{Binding Attachments}" Visibility="{Binding HasAttachments, Converter={StaticResource BoolToVisibility}}" Margin="0,11,0,0">
           <ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
           <ItemsControl.ItemTemplate><DataTemplate><Border Width="190" Height="128" CornerRadius="10" ClipToBounds="True" Background="{StaticResource PanelBrush}" Margin="0,0,8,8"><Image Source="{Binding FilePath}" Stretch="UniformToFill" ToolTip="{Binding FileName}"/></Border></DataTemplate></ItemsControl.ItemTemplate>
          </ItemsControl>
          <StackPanel Orientation="Horizontal" Margin="-7,9,0,0" Opacity="0.72">
           <Button Content="Copy" Style="{StaticResource GhostButtonStyle}" Padding="7,4" FontSize="11" Tag="{Binding}" Click="CopyMessage_OnClick"/>
           <Button Content="Edit" Style="{StaticResource GhostButtonStyle}" Padding="7,4" FontSize="11" Command="{Binding DataContext.BeginEditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
           <Button Content="Delete" Style="{StaticResource GhostButtonStyle}" Padding="7,4" FontSize="11" Tag="{Binding}" Click="DeleteMessage_OnClick"/>
           <StackPanel Orientation="Horizontal" Visibility="{Binding IsAssistant, Converter={StaticResource BoolToVisibility}}">
            <Button Content="↻" Style="{StaticResource GhostButtonStyle}" Padding="7,4" Command="{Binding DataContext.RegenerateCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" ToolTip="Regenerate"/>
            <Button Content="‹" Style="{StaticResource GhostButtonStyle}" Padding="7,4" IsEnabled="{Binding CanSwipeLeft}" Command="{Binding DataContext.SwipeLeftCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
            <TextBlock Text="{Binding SwipeDisplay}" VerticalAlignment="Center" Foreground="{StaticResource MutedTextBrush}" FontSize="11" Margin="2,0,4,0"/>
            <Button Content="›" Style="{StaticResource GhostButtonStyle}" Padding="7,4" Command="{Binding DataContext.SwipeRightCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
           </StackPanel>
          </StackPanel>
         </StackPanel>
         <StackPanel Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibility}}">
          <TextBox Text="{Binding EditText, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" MinWidth="380" MinHeight="88" MaxHeight="220" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
           <Button Content="Cancel" Style="{StaticResource GhostButtonStyle}" Command="{Binding DataContext.CancelEditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
           <Button Content="Save" Style="{StaticResource PrimaryButtonStyle}" Margin="0" Command="{Binding DataContext.SaveEditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
          </StackPanel>
         </StackPanel>
        </StackPanel>
       </Grid>
       <Border.Style>
        <Style TargetType="Border">
         <Setter Property="HorizontalAlignment" Value="Left"/><Setter Property="Background" Value="Transparent"/>
         <Style.Triggers>
          <DataTrigger Binding="{Binding Role}" Value="{x:Static models:ChatRole.User}">
           <Setter Property="HorizontalAlignment" Value="Right"/><Setter Property="Background" Value="{StaticResource CardBrush}"/>
          </DataTrigger>
         </Style.Triggers>
        </Style>
       </Border.Style>
      </Border>
      <DataTemplate.Triggers>
       <DataTrigger Binding="{Binding Role}" Value="{x:Static models:ChatRole.User}">
       <Setter TargetName="Avatar" Property="Background" Value="#D8DADF"/>
        <Setter TargetName="AvatarLetter" Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter TargetName="AvatarLetter" Property="Text" Value="Y"/>
       </DataTrigger>
      </DataTemplate.Triggers>
     </DataTemplate>
    </ListBox.ItemTemplate>
   </ListBox>
  </Grid>

  <!-- Floating GPT-style composer -->
  <Grid Grid.Row="3" Margin="24,0,24,18">
   <Grid MaxWidth="820" HorizontalAlignment="Stretch">
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
    <Border Background="#FDECEC" BorderBrush="#F4C5C5" BorderThickness="1" CornerRadius="8" Padding="11" Margin="0,0,0,8" Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibility}}">
     <TextBlock Text="{Binding ErrorMessage}" Foreground="#B42318" TextWrapping="Wrap"/>
    </Border>
    <Border Grid.Row="1" Background="White" BorderBrush="#C9CDD3" BorderThickness="1" CornerRadius="22" Padding="12,9">
     <Border.Effect><DropShadowEffect Color="#000000" BlurRadius="18" ShadowDepth="4" Opacity="0.12"/></Border.Effect>
     <Grid>
      <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
      <ItemsControl ItemsSource="{Binding PendingImagePaths}" Margin="2,0,0,7">
       <ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
       <ItemsControl.ItemTemplate><DataTemplate><Border Background="#ECEEF0" CornerRadius="8" Padding="8,5" Margin="0,0,7,0"><StackPanel Orientation="Horizontal"><TextBlock Text="{Binding}" MaxWidth="220" FontSize="11" TextTrimming="CharacterEllipsis"/><Button Content="×" Style="{StaticResource GhostButtonStyle}" Padding="4,0" Margin="6,0,0,0" Command="{Binding DataContext.RemovePendingImageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/></StackPanel></Border></DataTemplate></ItemsControl.ItemTemplate>
      </ItemsControl>
      <Grid Grid.Row="1">
       <TextBox x:Name="ComposerInputBox" Style="{StaticResource ComposerTextBoxStyle}" Padding="9,7" Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" MinHeight="42" MaxHeight="160" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" PreviewKeyDown="InputBox_OnPreviewKeyDown" AllowDrop="True" Drop="InputBox_OnDrop" ToolTip="Message NativeTavern · Enter to send · Shift+Enter for new line"/>
       <TextBlock Text="给 NativeTavern 发送消息" Foreground="{StaticResource MutedTextBrush}" FontSize="15" Margin="9,7" VerticalAlignment="Top" IsHitTestVisible="False">
        <TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding InputText}" Value=""><Setter Property="Visibility" Value="Visible"/></DataTrigger><DataTrigger Binding="{Binding InputText}" Value="{x:Null}"><Setter Property="Visibility" Value="Visible"/></DataTrigger><DataTrigger Binding="{Binding IsKeyboardFocused, ElementName=ComposerInputBox}" Value="True"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style></TextBlock.Style>
       </TextBlock>
      </Grid>
      <Grid Grid.Row="2" Margin="0,6,0,0">
       <Button HorizontalAlignment="Left" Content="＋" Style="{StaticResource RoundIconButtonStyle}" Width="32" Height="32" Background="#ECEEF0" BorderThickness="0" Click="AttachImage_OnClick" ToolTip="Attach image"/>
       <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <Button Content="■" Style="{StaticResource RoundIconButtonStyle}" Width="32" Height="32" FontSize="11" Background="#E3E5E8" BorderThickness="0" Command="{Binding StopCommand}" Margin="0,0,8,0" ToolTip="Stop"/>
        <Button Content="↑" Style="{StaticResource RoundIconButtonStyle}" Width="32" Height="32" Background="{StaticResource AccentBrush}" Foreground="White" BorderThickness="0" Command="{Binding SendCommand}" ToolTip="Send"/>
       </StackPanel>
      </Grid>
     </Grid>
    </Border>
    <TextBlock Grid.Row="2" Text="NativeTavern can make mistakes. Check important information." FontSize="10" Foreground="{StaticResource SubtleTextBrush}" HorizontalAlignment="Center" Margin="0,8,0,0"/>
   </Grid>
  </Grid>
 </Grid>
</UserControl>
