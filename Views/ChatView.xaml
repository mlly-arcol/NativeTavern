<UserControl x:Class="NativeTavern.Views.ChatView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:models="clr-namespace:NativeTavern.Models">
 <Grid Margin="28">
  <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
  <DockPanel Margin="0,0,0,18"><Button DockPanel.Dock="Right" Content="+ New Chat" Command="{Binding NewChatCommand}"/><StackPanel><TextBlock Text="Chat" FontSize="28" FontWeight="SemiBold"/><TextBlock Text="{Binding SessionTitle}" Foreground="{StaticResource MutedTextBrush}" Margin="0,5,0,0"/></StackPanel></DockPanel>
  <Border Grid.Row="1" Padding="14" Margin="0,0,0,14" CornerRadius="8" Background="#302A1E" Visibility="{Binding HasProviderConfiguration, Converter={StaticResource InverseBoolToVisibility}}"><DockPanel><Button DockPanel.Dock="Right" Content="Go to Settings" Command="{Binding OpenSettingsCommand}"/><TextBlock Text="尚未配置模型 Provider，请先前往 Settings。" VerticalAlignment="Center"/></DockPanel></Border>
  <ListBox x:Name="MessageList" Grid.Row="2" ItemsSource="{Binding Messages}" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
   <ListBox.ItemContainerStyle><Style TargetType="ListBoxItem"><Setter Property="HorizontalContentAlignment" Value="Stretch"/><Setter Property="IsHitTestVisible" Value="False"/><Setter Property="Padding" Value="0"/><Setter Property="Margin" Value="0,0,0,14"/></Style></ListBox.ItemContainerStyle>
   <ListBox.ItemTemplate><DataTemplate><Border Padding="16,12" CornerRadius="10" MaxWidth="720"><Border.Style><Style TargetType="Border"><Setter Property="HorizontalAlignment" Value="Left"/><Setter Property="Background" Value="{StaticResource CardBrush}"/><Style.Triggers><DataTrigger Binding="{Binding Role}" Value="{x:Static models:ChatRole.User}"><Setter Property="HorizontalAlignment" Value="Right"/><Setter Property="Background" Value="#3E3577"/></DataTrigger></Style.Triggers></Style></Border.Style><StackPanel><TextBlock Text="{Binding RoleLabel}" FontWeight="SemiBold" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,6"/><TextBlock Text="{Binding Content}" TextWrapping="Wrap" FontSize="15" LineHeight="22"/></StackPanel></Border></DataTemplate></ListBox.ItemTemplate>
  </ListBox>
  <StackPanel Grid.Row="3" Margin="0,16,0,0">
   <Border Background="#48262B" CornerRadius="6" Padding="10" Margin="0,0,0,10" Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibility}}"><TextBlock Text="{Binding ErrorMessage}" Foreground="#FFB8BE" TextWrapping="Wrap"/></Border>
   <TextBox Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" MinHeight="76" MaxHeight="180" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" PreviewKeyDown="InputBox_OnPreviewKeyDown" ToolTip="Enter 发送，Shift+Enter 换行"/>
   <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0"><Button Content="Stop" Command="{Binding StopCommand}"/><Button Content="Send" Command="{Binding SendCommand}" Background="{StaticResource AccentBrush}"/></StackPanel>
  </StackPanel>
 </Grid>
</UserControl>
